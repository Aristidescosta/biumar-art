import { BaseLayoutFooter } from './BaseLayoutFooter';
import { BaseLayoutHeader } from './BaseLayoutHeader';
import { BaseLayoutRoot } from './BaseLayoutRoot';

export const BaseLayoutPage = {
  Root: BaseLayoutRoot,
  Header: BaseLayoutHeader,
  Footer: BaseLayoutFooter
};